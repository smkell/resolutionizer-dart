library resolutionizerd.test;

import 'package:unittest/unittest.dart';

void main() {
  group('resolutionizerd: server for the resolutionizer application', () {
    test('does stuff...', () {
      expect(1, equals(1));
    });
  });
}